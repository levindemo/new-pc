# setup 

Generate key
```sh
ssh-keygen -t rsa -b 4096 -C "your_email@example.com"
```

Get pub key content
```sh
cat ~/.ssh/id_rsa.pub
```

New key and Paste content on https://github.com/settings/keys